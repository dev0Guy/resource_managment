[project]
name = "resource-managment"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "gymnasium>=1.3.0",
]

[dependency-groups]
dev = [
    "hypothesis>=6.152.2",
    "pytest>=9.0.3",
]
